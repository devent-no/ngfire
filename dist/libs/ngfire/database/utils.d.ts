/** Recursively all Date into Timestamp */
export declare function fromDate<D>(target: D): D;
/** Recursively all Date into Timestamp */
export declare function toDate<D>(target: D, dateKeys: string[], path?: string): D;
